package types

const (
	FROM       = "FROM"
	COPY       = "COPY"
	ENV        = "ENV"
	RUN        = "RUN"
	EXPOSE     = "EXPOSE"
	VOLUME     = "VOLUME"
	ENTRYPOINT = "ENTRYPOINT"
	CMD        = "CMD"
)
